% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unround.R
\name{bound_numerators}
\alias{bound_numerators}
\title{Rounding bounds as exact integers}
\usage{
bound_numerators(x_num, digits, rounding, threshold, symmetric)
}
\arguments{
\item{x_num}{Numeric (length 1). The rounded number.}

\item{digits}{Integer (length 1). Number of decimal places in \code{x_num}.}

\item{rounding}{String (length 1). Rounding method presumably used to create
\code{x_num}. See the \code{Rounding} section of \code{\link[=unround]{unround()}}.}

\item{threshold}{Numeric (length 1). The point within a step at which
rounding switches direction, in tenths of a step, for the \code{"up_from"},
\code{"down_from"}, and \code{"up_from_or_down_from"} methods. See
\code{\link[=round_up_from]{round_up_from()}}.}

\item{symmetric}{Logical (length 1). Whether the rounding of negative numbers
mirrored that of positive numbers. See \code{\link[=round_up]{round_up()}}.}
}
\value{
A list of five elements, or \code{NULL} if the bounds are undefined, which
is the case only for a missing \code{x_num}:
\itemize{
\item \code{lower}, \code{upper}: integer numerators of the two bounds.
\item \code{denom}: the common denominator.
\item \code{incl_lower}, \code{incl_upper}: whether each bound is inclusive.
}

Throws an error if \code{rounding} is not a known method.
}
\description{
\code{bound_numerators()} returns the same rounding bounds as
\code{\link[=unround]{unround()}}, but as exact integer numerators over a common denominator
rather than as decimal numbers. It is the low-level counterpart to
\code{unround()}, meant for building consistency tests on top of.

Most users want \code{\link[=unround]{unround()}} instead.
}
\details{
Deriving a set of candidate values from floating-point bounds is
unsafe. If a product such as \code{floor(upper * n)} is mathematically an exact
integer, its \code{double} representation can fall on either side of it, so a
legitimate candidate may be silently dropped or a phantom one admitted.
Either way, a consistency verdict can flip. See
\url{https://github.com/lhdjung/scrutiny/issues/86}.

Every bound that \code{unround()} can return is \code{x} plus a whole number of units
of \code{1 / 10^(digits + 1)}, and \code{x} itself is a whole number of such units
because it has \code{digits} decimal places. Both bounds therefore have exact
integer numerators over \code{10^(digits + 1)}, and a comparison of some value
to a bound becomes a comparison between integers, which is exact in double
precision as long as every integer involved stays below \code{2^53}.

A consistency test that derives its candidate range from here can never
disagree with \code{unround()} about what the bounds of a rounded number are.

If \code{threshold} is fractional, the numerators and the denominator are both
scaled up by a power of ten until they are whole numbers again.
}
\examples{
# The bounds of 2.7, rounded up or down from 5,
# are 2.65 and 2.75 -- here as 265/100 and 275/100:
bound_numerators(
  x_num = 2.7,
  digits = 1,
  rounding = "up_or_down",
  threshold = 5,
  symmetric = FALSE
)

# `unround()` shows the same bounds as decimals:
unround("2.7")
}
\seealso{
\code{\link[=unround]{unround()}}, which presents the same bounds as decimal numbers in
a tibble.
}
